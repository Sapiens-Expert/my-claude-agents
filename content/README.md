# Content

Content inventory and production state. Research belongs in `research/`; canonical voice in `voice/`; platform rules in `platforms/`. Never place private client information in publishable folders.

